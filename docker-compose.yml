name: teamtasks
services:
    devopsteamsdb:
        ports:
            - 5000:5000
        image: devopsteamsdb/devopsteamsdb:teamtasks_latest

version: '3'
services:
  myservice:
    image: devopsteamsdb/devopsteamsdb:teamtasks_latest
    ports:
      - "5000:5000"
