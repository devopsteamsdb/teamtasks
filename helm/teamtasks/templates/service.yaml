apiVersion: v1
kind: Service
metadata:
  name: {{ include "teamtasks.fullname" . }}
  labels:
    {{- include "teamtasks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "teamtasks.selectorLabels" . | nindent 4 }}
